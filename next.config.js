/** @type {import('next').NextConfig} */
const nextConfig = {};
module.exports = {
  images: {
    domains: ["contents.kyobobook.co.kr"], // 외부 이미지를 가져올 수 있는 도메인 추가
  },
};
module.exports = nextConfig;
